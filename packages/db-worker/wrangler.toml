name               = "metricade-db-worker"
main               = "src/index.ts"
compatibility_date = "2025-09-01"
workers_dev        = true

routes = [
  { pattern = "db.metricade.com", custom_domain = true }
]

[vars]
CLICKHOUSE_HOST = "https://y390vosagc.us-east1.gcp.clickhouse.cloud:8443"

# Secrets (wrangler secret put — never stored here):
# INGEST_SHARED_SECRET
# CLICKHOUSE_PASSWORD
