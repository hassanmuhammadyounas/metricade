{
  "name": "@behavioral-intelligence/edge-worker",
  "version": "1.0.0",
  "description": "Cloudflare Worker — behavioral event ingestion API (Hono)",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "./scripts/deploy.sh",
    "test": "vitest run",
    "test:watch": "vitest",
    "type-check": "tsc --noEmit"
  },
  "dependencies": {
    "hono": "^4.12.5"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260228.1",
    "typescript": "^5.9.3",
    "vitest": "^4.0.18",
    "wrangler": "^4.70.0"
  }
}
